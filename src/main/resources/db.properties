db.driverClassName=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/dayflow_hrms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=root

# Fallback H2 configuration (in case MySQL is not running or for testing)
h2.driverClassName=org.h2.Driver
h2.url=jdbc:h2:mem:dayflow_hrms;DB_CLOSE_DELAY=-1;MODE=MySQL
h2.username=sa
h2.password=
